# GreenHorn 产品需求文档（PRD）v0.1

> 主导发起人 / PM：小菠萝（WorkBuddy 助手）
> 产品 Owner：用户（终端用户视角，非程序员）
> 文档日期：2026-07-28 ｜ 状态：草案，待 Owner 确认后分派开发

---

## 0. 项目基本信息

| 项 | 内容 |
|----|------|
| 项目英文名 | **GreenHorn** |
| 含义 | 英文 "greenhorn" = 生手 / 小白 / 什么都不懂的门外汉（对应中文"小白"） |
| 定位 | 给普通终端用户（非程序员）开箱即用的开源智能体**产品化外壳** |
| 底层引擎（MVP） | 单一引擎 **PI**（开源编码智能体，GitHub: earendil-works/pi） |
| 形态（MVP） | **本地网页**（用户机器上起服务 + 浏览器打开），桌面应用后续再说 |
| 项目目录 | `D:\program\GreenHorn`（与 `D:\program\pi-agent` 平级） |
| 对外对接材料目录 | `D:\program\GreenHorn\交流文件`（给外部开发者/合作方看的文档放这里） |
| 引擎源码位置 | `D:\program\pi-agent\source`（PI 完整工程）<br>`D:\program\pi-agent\pi-web-switch`（已有的只读管理面板，可复用基座）<br>`D:\program\pi-agent\工程文件`（auth.json / models.json / settings.json 真实配置） |

---

## 1. 背景与要解决的问题

用户作为终端使用者，在用 Trae / QClaw / WorkBuddy / Dumate 等闭源产品，以及 PI / openclaw / Hermes 等开源智能体时，发现两类痛点：

1. **闭源产品**赢在"双击即用"，但部分对小白不友好（配置项、模型调用、VPN/Key 等问题解释不清）。
2. **开源项目**能力不差，但卡在**部署门槛**：要自己下前后端、看仓库说明、配环境、敲命令启动——对非程序员极不友好，尤其大量 CLI 模式项目。

**GreenHorn 的目标**：用开源 PI 当引擎，做一层"开箱即用的网页外壳"，把上面的门槛全部藏起来——用户打开网页，选项目、按引导填好模型 Key、用默认值，直接就能跟智能体对话干活。本质是**开源智能体的"发行版 / 产品化"**。

---

## 2. 产品目标与 MVP 边界

### 做（MVP）
- 一个本地网页，打开即用。
- 选开源项目（MVP 仅 PI）→ 后端从**国内镜像自动拉取**到用户电脑（不用本人去 GitHub 找、不用翻墙）。
- 模型 API **分步引导教程**（MVP 先 DeepSeek，后续补智谱/通义/豆包）。
- 每个按钮/功能旁**问号 tooltip 帮助**，悬停弹说明；设置里可整体开关"说明"。
- 关键配置项给**默认值 + 说明**（上下文长度、温度等）。
- 一个能**真正跟 PI 对话**的界面（发一句话 → 看到它思考、调工具、出结果），而不是只读面板。

### 不做（MVP 明确排除）
- 桌面应用打包（后续阶段）。
- 同时接多个引擎（仅 PI；但架构预留扩展口）。
- 模型训练 / 微调。
- 云端多租户部署（MVP 纯本地单机）。

---

## 3. 目标用户

- 非程序员、不懂命令行/编程思维的**普通终端用户**。
- 想用开源智能体但被"下载/配置/启动"劝退的人。
- 用户自己就是典型用户，决策以"小白能否零障碍上手"为第一标准。

---

## 4. 核心功能清单（MVP）

| 编号 | 功能 | 说明 | 优先级 |
|------|------|------|--------|
| F1 | 项目选择 + 镜像拉取 | 网页列出可选项目（MVP 仅 PI）；用户点选后，**后端自动从国内镜像拉取源码+依赖+模型**到本机，全程无需用户去官网 | P0 |
| F2 | 模型 API 引导教程 | 选完项目后进入引导：按模型分套教程（MVP=DeepSeek），告诉用户"去哪个网站、点哪、在哪生成 API Key、把 Key 和 URL 填到哪个框" | P0 |
| F3 | 对话界面（驱动 PI） | 发送自然语言 → 网页把指令发给 PI → 流式收回 PI 的回复、思考过程、工具调用、最终产物（代码/文件） | P0 |
| F4 | 配置面板（复用基座） | 模型选择、默认 Provider/模型、API Key 管理、扩展管理、配置导入导出——**复用 pi-web-switch 已有的读取 `~/.pi/agent/` 的 UI 基座** | P1 |
| F5 | 问号 tooltip 帮助 | 每个按钮/输入项旁小圆圈(?)；鼠标悬停弹说明框；设置里可开/关"说明"，关掉则圆圈消失 | P1 |
| F6 | 默认值 + 说明 | 上下文长度、温度等给初始默认值，并附一句大白话说明 | P1 |
| F7 | 设置（说明开关/主题） | 全局开关"显示说明"；浅色/深色主题 | P2 |
| F8 | 会话浏览 | 查看历史会话（可复用 pi-web-switch 的会话浏览能力） | P2 |

---

## 5. 用户使用流程（主路径）

```
1. 用户打开 GreenHorn 网页（本地）
2. 首页：选择要用的开源项目 → 选「PI」
3. 系统检测本机是否已有 PI：
   - 没有 → 后端从国内镜像自动拉取（源码+依赖+模型），显示进度
   - 已有 → 跳过
4. 进入「模型设置」引导：
   - 推荐：先用本地 Ollama（零 Key 零费用）或 引导填 DeepSeek
   - 选 DeepSeek → 弹出分步教程：去 platform.deepseek.com → 新建 API Key → 复制
   - 回到网页把 Key 粘贴进对应框，URL 已预填好
5. 设置面板：默认值已填好（上下文长度/温度等），不熟悉可悬停问号看说明
6. 进入对话界面：输入「帮我整理本周竞品动态并出 PPT」
   → 网页驱动 PI 执行，实时显示思考+工具调用+产物
7. 随时可在设置里关掉"说明"圆圈（熟悉后）
```

---

## 6. 信息架构（页面）

- **首页 / 项目选择**：列出可选引擎（MVP=PI），显示是否已安装、安装进度。
- **引导 / 教程页**：按所选模型展示分步图文教程（MVP=DeepSeek）。
- **设置面板**（复用 pi-web-switch 基座）：模型、Provider、Key、扩展、默认值、说明开关、主题。
- **对话工作台**（MVP 重点新增）：消息流（文本 + 工具调用卡片 + 产物预览）、输入框、运行/停止。
- **会话/记忆浏览**（可选，复用基座能力）。

---

## 7. 技术方案（暂行假设 + 待验证项）

> 本节是 PM 基于现有工程侦察给出的**暂行方向**，标 ⚠️ 的是必须在开工前验证的关键未知。

### 7.1 网页如何真正"驱动 PI"对话（⚠️ 最关键未知）
PI 本质是 CLI/TUI（`pi` 命令）。要让网页驱动它，有三种候选机制，需先验证哪一个可行：

- **(a) PI 是否内置 server / headless 模式**：查 `pi-coding-agent` 与上游 `pi-web-ui` 包——上游已有 `pi-web-ui`，说明官方可能已实现"网页↔Agent 传输"，**优先验证能否复用**。
- **(b) 进程管道**：网页后端起 `pi` 进程，把 prompt 经 stdin 喂入，捕获 stdout（含思考/工具调用），按 PI 的输出格式解析成流式消息。
- **(c) 复用 pi-web-switch 的 server 模式**：它已有 `server/pi-reader.ts` 后端，但当前只"读"不"写/驱动"；需扩展成"发指令给 PI + 收流式回传"。

**PM 建议**：先验证 (a)/(c)，能复用官方/现有代码最省事、最稳（符合"优先成熟方案、不自研"原则）。

### 7.2 配置层（复用 pi-web-switch 基座）
- PI 的真实配置在 `~/.pi/agent/`（`auth.json` / `models.json` / `settings.json`）。
- pi-web-switch 已证明：前端 + 一个 Node 后端（`server/pi-reader.ts`）**直接读写这些文件**即可，无需额外数据库。
- **GreenHorn 直接复用此模式**：配置 UI 基于 pi-web-switch 改造，新增的"镜像拉取"和"对话驱动"作为新模块叠加。

### 7.3 镜像自动拉取（F1）
- 后端负责三层拉取：① 源码（国内 Git 镜像）② 依赖（pip/npm 国内镜像源）③ 模型（如本地 Ollama 模型，国内可下镜像）。
- 仅 MVP 做 PI；镜像地址配置化，后续加引擎只需加镜像条目。
- 拉取后自动执行"装依赖 + 起服务"的一键脚本，避免用户敲命令。

### 7.4 模型：DeepSeek 优先（F2）
- PI 走 **OpenAI 兼容 API**（`baseUrl` + API Key）。DeepSeek 在 pi-web-switch 里已是**内置提供商**（DeepSeek V3 / R1），教程可直接套用。
- 引导教程按模型分套：MVP 写 DeepSeek 一套（去哪申请、URL 填什么、Key 从哪复制）；后续补智谱/通义/豆包。
- 另提供**零 Key 备选**：本地 Ollama（PI 默认 qwen3.5:9b），不想申请 Key 的用户可直接用。

### 7.5 问号 tooltip + 设置开关（F5/F7）
- 纯前端能力：每个交互元素旁渲染 `?` 图标，hover 显示说明（文案存配置表，便于维护）。
- 设置里一个总开关 `显示说明`；关闭则所有 `?` 不渲染。

---

## 8. 关键技术风险与待验证

| 风险 | 说明 | 缓解 |
|------|------|------|
| ⚠️ PI 对话驱动机制不明 | 7.1 三种机制未验证哪种可行 | 开工前先读 `pi-coding-agent` / `pi-web-ui` 源码与文档，确定机制后再定后端架构 |
| 镜像稳定性 | 国内镜像可能失效/不全 | 镜像地址配置化，保留多个备用源；拉取失败有清晰报错引导 |
| 模型 Key 安全 | Key 存本机 `auth.json` | 明确"仅存本机、不上云"；可加"不保存 Key"选项 |
| 跨平台 | MVP 先 Windows（用户环境） | 一键脚本用 PowerShell；后续扩 macOS/Linux |

---

## 9. 开发节点计划（里程碑）

| 阶段 | 内容 | 产出 | 负责 |
|------|------|------|------|
| M0 技术验证 | 确定"网页驱动 PI"的机制（7.1） | 验证报告 + 架构定稿 | 专业前端（PM 给侦察结论） |
| M1 基座复用 | 基于 pi-web-switch 跑通配置 UI（模型/Key/设置） | 可运行的配置面板 | 专业前端 |
| M2 镜像拉取 | 后端实现选项目→镜像拉取→自动装依赖起服务 | F1 可用 | 专业前端 |
| M3 对话驱动 | 后端对接 PI 对话通道 + 前端对话界面（流式） | F3 可用 | 专业前端 + PM 定契约 |
| M4 引导教程 | DeepSeek 分步引导 + 其他模型教程框架 | F2 可用 | UI + PM 写教程文案 |
| M5 帮助系统 | tooltip 帮助 + 默认值说明 + 设置开关 | F5/F6/F7 | UI + 前端 |
| M6 联调打磨 | 端到端小白流程走通、文案校对 | MVP 验收 | 全体 |

> 分工：专业前端 + 专业 UI 做编码；PM（小菠萝）负责计划/节点/workflow/接口契约/审计。

---

## 10. 给开发者的接口契约（概要，待 M0 后细化）
- **统一内部消息模型**：`{sessionId, role, type: text|tool_call|artifact, content, ts}`，前端与后端都用这套，屏蔽各引擎差异（为未来多引擎预留）。
- **配置读写契约**：直接操作 `~/.pi/agent/` 下的 `auth.json` / `models.json` / `settings.json`，格式沿用 PI 现有 schema。
- **镜像拉取 API**（后端内部）：`GET /api/projects` 列表；`POST /api/projects/:id/install` 触发拉取，返回进度流。
- **对话 API**：`POST /api/chat`（发 prompt），`GET /api/chat/stream` 或 WebSocket 收流式事件。

---

## 11. 多引擎扩展预留
- 所有"引擎相关"逻辑收敛到 **Adapter 层**（MVP 仅 `pi-adapter`）。
- 未来加 openclaw / Hermes = 新增一个 adapter + 一条镜像配置，前端/对话/帮助系统不重复开发。
- 内部消息模型（第 10 节）与引擎解耦，是扩展的基石。

---

## 待 Owner 确认
1. 项目名 **GreenHorn** 是否采用（可改名）。
2. 是否认可"复用 pi-web-switch 基座 + 新增对话驱动/镜像拉取/教程层"的技术路线。
3. 是否授权开发者在 M0 阶段直接读 `D:\program\pi-agent` 源码做机制验证。
4. 模型引导顺序：DeepSeek（首）→ 之后补哪几个（智谱/通义/豆包）？
