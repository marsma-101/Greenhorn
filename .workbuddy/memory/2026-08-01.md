# 2026-08-01 工作日志

## 6 大智能体源码全部下载到 D:\program\
- DeepSeek 官方文档 6 个智能体 + 本机已有 PI，全部源码就位：
  - ✅ 直连下载：claude-code、codex、openclaw
  - ✅ 镜像下载（gh-proxy.com）：opencode、reasonix（直连被 reset）
  - ✅ 已有：pi-agent（D:\program\pi-agent）、hermes-agent（用户手动移入）
- 结论：GitHub 直连不稳定，拉取代码需内置镜像降级（gh-proxy.com → ghfast.top）
- 调研记录更新：`D:\ai使用培训\GreenHorn拉取参考\智能体拉取链接调研记录.md`

## 智能体拉取链接调研（重要参考资产）
- 发现 DeepSeek 官方文档列出 6 个可接入智能体：Claude Code / Codex / Hermes / OpenClaw / OpenCode / Reasonix
- 逐一实测（本机未开代理）：
  - ✅ 直连可用：Claude Code（npm）、Codex（cdn.deepseek.com 脚本）、OpenClaw（openclaw.ai）、OpenCode（opencode.ai）、Reasonix（npm）
  - ⚠️ Hermes：raw.githubusercontent.com 偶发超时，重试可通；镜像 gh-proxy.com / ghfast.top 可用
- 记录保存：`D:\ai使用培训\GreenHorn拉取参考\智能体拉取链接调研记录.md`（**项目外，不会被 git 追踪**）
- 用途：GreenHorn 后期"自动拉取智能体引擎"功能的下载源参考
- 待办：开代理后补测镜像稳定性、Hermes 脚本内部依赖、OpenCode 安装包直链

## 第二批需求已发出（模板管理 + 技能管理）
- 提示词模板：列表/新建（自动生成参照模板）/编辑/删除，直接编辑 md
- 技能库：4 扩充入口（AI 生成/线上/本地/扫描外部文件夹）+ 自动/手动调用模式 + 对话页 🧰 手动调用按钮 + 含代码技能安全提示
- 文件：2026-07-31-2210-PM-指令-第二批模板与技能管理.md

## 安装路径自定义需求（用户明确：程序不装 C 盘）
- 当前 setup.ts 安装路径写死 `~/.pi/agent`（C 盘），无选路径步骤
- 需求：安装引导新增「选安装位置」步骤，默认推荐 D 盘，程序+配置+会话全部跟随用户选择路径
- 含旧配置迁移提示
- 文件：2026-08-01-0940-PM-指令-安装路径自定义.md

## 数据迁移需求（重装系统不丢数据，用户重点关注）
- 要迁移的数据：settings/auth(API Key)/prompts(模板)/skills(技能)/sessions(聊天记录)/themes/extensions
- 三种场景：①安装时检测旧数据提示迁移 ②设置页手动迁移+备份zip ③重装后恢复引导
- 已补充进安装路径自定义需求文件

## 引擎安装位置管理 v2（用户最终拍板方案）
- **引擎统一装项目内 `ai-agent/` 文件夹**（GreenHorn/ai-agent/<引擎名>/），配 README.md 说明各程序位置
- **程序自身位置检测**：GreenHorn 在 C 盘时启动弹窗提醒，引导移动到其他盘符（防 C 盘占用 + 重装丢数据）
- **选引擎时检测**：引擎不在默认位置 → 弹窗问迁移/不迁移（不迁移则记住自定义位置）
- **迁移三件套**（防数据丢失关键）：①程序本体 ②数据目录 ③环境变量注入（PI_CODING_AGENT_DIR 覆盖引擎内部默认路径）——只搬程序必丢数据
- 保留用户自定义安装位置能力（可选）
- 文件：2026-08-01-1116-PM-指令-引擎安装位置管理v2.md（旧版 0940 已归档）

## 删除首页 Ollama 入口（用户拍板：新手老手都不显示）
- 首页/引擎选择流程彻底移除 Ollama 卡片和入口（小白没装会混乱）
- **保留**：设置页 ⚙️ 模型来源 Ollama provider、模型列表、对话链路、验证接口（防误删清单已写入）
- 补充：**首页 6 张智能体卡片**（PI/Hermes/Claude Code/Codex/OpenCode/Reasonix），每张有名称+一句话介绍+状态徽标，未装点击走安装引导
- 文件：2026-08-01-1124-PM-指令-删除首页Ollama入口.md

## 三需求执行计划已批准（Trae 计划 + PM 确认）
- Trae 计划：S1（删 Ollama + 6 引擎卡片 UI）→ S2（ai-agent 基础设施 + C盘检测 + 迁移）→ S3（模板+技能），8 天
- 关键架构冲突：R2 数据目录迁移 vs 已验收的 PI 会话架构 → **PM 拍板方案 A**（会话路径迁到 ai-agent/pi/data/sessions/，需重验收）
- PM 补充：①S1 完成后先看布局再进 S2 ②会话迁移验证 jsonl 格式一致、cwd 不双重转义 ③迁移"复制不删除" ④环境变量兼容性逐个引擎实测 ⑤S3 按新路径写
- 文件：2026-08-01-1245-PM-确认-三需求执行计划.md
- 已归档 Trae 读过的 3 份需求文件

## S1 验收通过 + 指令继续 S2/S3 一次交付
- S1 交付（首页 6 引擎卡片 + 路由改造）PM 实测 `/api/pi` 正常 → ✅ 验收通过
- 用户要求：不再分阶段验收，**S2+S3 连续做完一次性交付**
- 新指令强调：会话迁移验证（jsonl 格式一致/cwd 不双重转义/PI /resume 可读）、环境变量逐引擎实测、复制不删除、S3 用新路径
- 文件：2026-08-01-1300-PM-指令-继续S2S3一次交付.md

## S2+S3 交付验收不通过（P0 bug）
- Trae 一次性交付 S2（ai-agent 基础设施）+ S3（模板+技能），PM 实测：
  - ✅ 通过：ai-agent 目录、会话迁移（5 会话、格式正确、cwd 无双重转义）、引擎状态 API、位置检测
  - ❌ **P0：所有 POST 接口崩溃**（chat/模板创建/安全检测/扫描）——根因 `index.ts` 缺 `app.use(express.json())` 中间件（S2 重构时弄丢，git 历史 bd9e478 有）
  - 影响：对话功能完全不可用
- 文件：2026-08-01-1330-PM-验收不通过-P0中间件缺失.md

## S2+S3 二次验收通过 ✅（P0 已修复）
- Trae 修复：index.ts 加回 `cors()` + `express.json()`（2 行）
- PM 实测：模板创建/删除 ✅、安全检测（high 风险正确检出）✅、技能扫描 ✅、会话创建/删除 ✅、对话 SSE 流 ✅
- 遗留：Ollama 对话端点 404（chat-engine URL 拼接问题，非中间件）；DeepSeek 链路已通（假 Key 返回正确错误）
- 归档：返工交付 + 验收报告已归档，根目录只留常驻文档

## 网页安装器需求（用户想从头测试项目）
- 用户要做"模拟用户全新安装"测试：单个 HTML 安装器，从 **GitHub/镜像**（非本地）拉取公开仓库 greenhorn
- 方案 C 混合：①下载 zip ②下载 install.bat（自动 clone+装依赖+启动）③复制 git 命令
- 拉取降级链：GitHub 直连 → gh-proxy.com → ghfast.top（参考调研记录）
- 待 Trae 确认：仓库 owner、默认分支
- 文件：2026-08-01-1420-PM-指令-网页安装器.md

## 多引擎适配 + 多智能体协作战略计划（✅ 已立项）
- 用户拍板：①先实现所有项目拉取安装功能（A 系列），协作排后（B 系列）②指挥官用户自选（推荐 PI/Reasonix）③对话式 UI ④立项
- A 系列：A1 引擎接入手册 → A2 网页安装器 → A3 引擎拉取安装到 ai-agent → A4 安装向导 UI → A5 引擎验证
- B 系列：B1 适配层(PI+Hermes) → B2 铺开 → B3 任务协议 → B4 指挥官协作 → B5 验收闭环
- 文件：2026-08-01-1434-PM-计划-多引擎适配与多智能体协作.md

## A 系列开工（用户二轮决策）
- 决策：A 全部完成验收后再做 B；**Trae 可派子智能体分别写各引擎拉取功能**，主智能体整合验证；手册+完成报告主智能体写；验证引擎用 **Hermes**
- A1 引擎接入手册（docs/引擎接入手册.md，7 引擎：启动/SDK/配置/数据目录/能力/难点）
- A3 引擎拉取安装（POST /api/engines/:id/install，GitHub→gh-proxy→ghfast 降级，子智能体每引擎一个模块）
- 文件：2026-08-01-1555-PM-指令-A1引擎手册与A3拉取安装.md
