# PI 引擎原生能力清单

> 分析日期: 2026-08-02
> 依据: docs/引擎分析/PI-代码功能分析.md
> 原则: 只列引擎原生代码直接实现的能力，排除 MCP/Skill 实现的

---

## 1. 核心对话能力

| 功能 | 支持 | 接口/实现 | 文件路径 | 触发方式 | 备注 |
|------|------|----------|---------|---------|------|
| 流式对话 | ✅ | `streamChat()` | `services/chat-engine.ts:25-160` | `stream: true` 默认开启 | SSE `data:` 行解析 |
| 思考过程 | 🟠 模拟 | 前20字符标记 | `chat-engine.ts:134-138` | 自动触发 | 非真正模型reasoning |
| 多模型支持 | ✅ | PROVIDER_URLS映射 | `routes/chat.ts:73-77` | 设置页切换 | 7供应商动态选择 |
| Token统计 | ❌ | — | — | — | — |
| 上下文压缩 | ✅ | `compaction.enabled` | `settings.json` | 用户配置 | reserveTokens保留 |

## 2. 原生工具能力

| 功能 | 支持 | 接口/实现 | 文件路径 | 触发方式 | 备注 |
|------|------|----------|---------|---------|------|
| 文件读取（注入） | 🟠 | `processFiles()` | `routes/chat.ts:51-63` | 前端上传 | 白名单扩展名+500KB限制 |
| 文件写入 | ❌ | — | — | — | — |
| 脚本/终端执行 | ❌ | — | — | — | — |
| 代码搜索 | ❌ | — | — | — | — |
| 任务规划 | ❌ | — | — | — | — |

## 3. 内置特殊工具

| 功能 | 支持 | 接口/实现 | 文件路径 | 触发方式 | 备注 |
|------|------|----------|---------|---------|------|
| 浏览器控制 | ❌ | — | — | — | — |
| 图像生成 | ❌ | — | — | — | — |
| 视觉分析 | ❌ | — | — | — | — |
| 语音/TTS | ❌ | — | — | — | — |
| 实时语音对话 | ❌ | — | — | — | — |
| 桌面控制 | ❌ | — | — | — | — |

## 4. 会话/记忆能力

| 功能 | 支持 | 接口/实现 | 文件路径 | 触发方式 | 备注 |
|------|------|----------|---------|---------|------|
| 会话持久化 | ✅ | JSONL写入 | `routes/sessions.ts:57-93` | 自动保存 | JSONL v3格式 |
| 会话搜索 | ✅ | 目录遍历 | `routes/sessions.ts` | API调用 | 按cwd分目录 |
| 会话改名 | ✅ | `session_info`事件 | `routes/sessions.ts` | API调用 | 写入JSONL |
| 会话删除 | ✅ | 文件删除 | `routes/sessions.ts` | API调用 | 删除JSONL文件 |
| 记忆系统 | ❌ | — | — | — | — |
| 会话分叉/恢复 | ✅ | 读取JSONL | `routes/sessions.ts` | API调用 | parentId链 |

## 5. 协作/网关能力

| 功能 | 支持 | 接口/实现 | 文件路径 | 触发方式 | 备注 |
|------|------|----------|---------|---------|------|
| 多Agent协作 | ❌ | — | — | — | — |
| IM Bot网关 | ❌ | — | — | — | — |
| 定时任务 | ❌ | — | — | — | — |
| 看板协作 | ❌ | — | — | — | — |

## 6. 独有原生能力

| 功能 | 支持 | 接口/实现 | 文件路径 | 触发方式 | 备注 |
|------|------|----------|---------|---------|------|
| 双通道架构 | ✅ | PIAdapter + chat-engine | `adapters/pi-adapter.ts` + `services/chat-engine.ts` | 自动路由 | 嵌入+独立Agent |
| 技能触发注入 | ✅ | `collectTriggeredSkills()` | `routes/chat.ts:32-49` | 按trigger匹配 | 关键词匹配注入prompt |
| 智能错误提示 | ✅ | 5类错误分类 | `services/chat-engine.ts:66-96` | 错误发生时 | auth/model/rate-limit/balance |
| 环境变量驱动目录 | ✅ | `initDataPaths()` | `services/pi-config.ts:52-70` | 启动时 | PI_CODING_AGENT_DIR |
| 配置自动迁移 | ✅ | `migrateOldSessions()` | `routes/sessions.ts:220-287` | 启动时 | v2→v3格式迁移 |