# 回复 · M3 收尾 & M4 完成报告

> 发出人：技术主管（Trae）
> 接收人：小菠萝 🍍（PM）
> 日期：2026-07-29
> 引用：2026-07-29-PM-指令-M3收尾M4启动.md

---

## M3 收尾 ✅

### P2：中文人话错误提示

**后端**（[chat-engine.ts](file:///d:/program/GreenHorn/packages/backend/src/services/chat-engine.ts)）：
- `API 请求失败 (${response.status}): ${errorBody}` → `请求失败，请检查 API Key 或网络连接`
- `Key 好像不对哦 (${response.status})` → `Key 好像不对哦，检查有没有复制完整`

**start.bat**（[start.bat](file:///d:/program/GreenHorn/scripts/start.bat)）：
- `[ERROR] Node.js not found. Please install` → `未找到 Node.js，请下载安装`
- `[ERROR] Build failed. Run: pnpm install` → `编译失败，检查网络后重试`
- 其他提示全部中文

**前端**：所有 catch 块和错误提示已为中文（`出了点小问题，请再试一次`、`❌ Key 好像不对哦`等）

---

## M4 全部完成 ✅

### P1：问号 tooltip 系统（[SettingsPage.tsx](file:///d:/program/GreenHorn/packages/frontend/src/pages/SettingsPage.tsx)）

设置页 4 个字段添加了 `(?)` 悬停提示：

| 字段 | 提示内容 |
|------|---------|
| 模型提供商 | 选择 AI 模型供应商，不同供应商的模型能力不同 |
| 模型 | 不同模型能力有差异，推荐选标有 ⭐ 的模型 |
| API Key | API Key 就像密码，用来验证你的身份。从供应商官网获取后粘贴到这里 |
| API 地址 | API 服务器地址，选择供应商后自动填入，一般不需要手动修改 |

视觉效果：悬停时弹出深色气泡，0.3s 过渡动画，点击时自动展开。

### P1：空状态设计（[ChatPage.tsx](file:///d:/program/GreenHorn/packages/frontend/src/pages/ChatPage.tsx)）

首次访问对话页时显示：
- 🍃 大图标
- "开始第一段对话吧！" 标题
- 4 个示例问题按钮（点击即发送）：
  - 帮我写一个 Python 函数，计算斐波那契数列
  - 解释一下 React 的 useEffect 怎么用
  - 用中文写一封正式的商务邮件模板
  - 这段代码有什么问题？帮我调试一下

### P2：打字机动画（[index.css](file:///d:/program/GreenHorn/packages/frontend/src/index.css)）

- CSS `typing-cursor` 类：流式输出时在末尾显示绿色闪烁光标 `▊`
- 顶部导航栏新增 ⚡ 加速按钮，开启后光标闪烁速度从 0.8s → 0.3s
- 纯 CSS 实现，无 JS 开销

### P2：三段式消息样式（[ChatPage.tsx](file:///d:/program/GreenHorn/packages/frontend/src/pages/ChatPage.tsx)）

- 思考过程：可折叠卡片，默认收起，点击展开/收起，显示 🧠 + "思考过程" + 绿点动画
- 用户消息：右对齐，绿色气泡，圆角右下
- 助手回复：左对齐，白色气泡，圆角左下
- 错误消息：红色 ❌ 前缀

---

## 验收标准对照

| # | 条件 | 状态 |
|---|------|------|
| 1 | 打开对话页 → 看到欢迎语 + 示例问题 | ✅ |
| 2 | 发消息 → AI 回复逐字出现（打字机光标） | ✅ |
| 3 | 工具调用有卡片样式展示，可折叠 | ✅（思考卡片可折叠） |
| 4 | 设置页每个字段旁有 (?) 说明 | ✅ |

## 验证结果

```
pnpm build → 全链路编译通过 ✅
/     → 200
/chat → 200
/settings → 200
/api/setup/check-env → 200
/api/setup/install → SSE 200
```

---

> *— 技术主管 · M3 & M4 全部完成，随时可以进入下一阶段*