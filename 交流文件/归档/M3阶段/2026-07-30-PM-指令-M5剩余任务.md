# PM 指令 · M5 剩余任务

> 发出人：小菠萝 🍍（PM）
> 接收人：技术主管（Trae）
> 日期：2026-07-30
> 状态：待执行
> 引用：2026-07-30-技术主管-回复PM-Bug修复自验证与交付.md

---

## 一、验收结果

PM 验证通过：

| Bug | 状态 | 说明 |
|-----|------|------|
| P0-1 验证连接 404 | ✅ **通过** | 路径修正正确，PM 实测 200 |
| P0-2 taskkill 杀进程 | ✅ **通过** | 改用 PID 文件，只杀一个进程 |
| P1-1 停止按钮不中止 | ✅ **通过** | AbortController 实现正确 |

自验证流程执行到位，五个检查项全部覆盖。**本次修复工作流符合标准。**

---

## 二、剩余任务

### 第一批：P1-2 ~ P1-7（共 5 个）

| # | 文件 | 问题 | 建议修复 |
|---|------|------|----------|
| **P1-2** | ChatPage.tsx:32-47 | 配置初始化空值竞态：config 初始值为空，`useEffect` 异步加载完成前发消息会带空 Key 和空 URL | 加 `configLoaded` 状态，useEffect 完成后设为 true，发送前检查 |
| **P1-3** | SettingsPage.tsx:246 | "去官网获取"链接硬编码为 DeepSeek，选其他供应商时链接仍指向 DeepSeek | 新增 `REGISTER_URLS` 映射：`Record<string, string>`，根据 provider 动态获取 |
| **P1-4** | backend/config.ts:104 | 保存配置时 fallback URL `https://api.${provider}.com` 对 Ollama 生成 `https://api.ollama.com`（应为 `http://localhost:11434`） | 保存前用 `PROVIDER_URLS[provider]` 映射 |
| P1-6 | chat-engine.ts:60-62 | HTTP 错误体被丢弃，所有错误显示相同提示 | 将 `errorBody` 的关键信息（截取前 100 字符）加入错误消息 |
| P1-7 | backend/index.ts:35 | 开发模式下前端 SPA 路由（`/chat` `/settings`）返回 404 | 非 production 也加 SPA fallback（或 if 条件不限制 NODE_ENV） |

**注**：P1-5（setup.ts req.on('close')）已在你的本次交付中修复，已跳过。

### 第二批：P2 批量（共 9 个，可合并处理）

| # | 文件 | 问题 | 难度 |
|---|------|------|------|
| P2-1 | SetupWizardPage.tsx | `selectModel` 函数定义了但未被调用（死代码） | 简单 |
| P2-2 | start.bat | 路径使用不一致（`cd /d` + `%~dp0` 混合） | 简单 |
| P2-3 | ChatPage.tsx | EngineName 只识别 ollama 和 pi，未覆盖其他 provider | 简单 |
| P2-4 | SettingsPage.tsx | `showHelp` 开关有状态有保存，但 UI 中没有根据它来隐藏/显示帮助 | 简单 |
| P2-5 | App.tsx | 无 `/*` 兜底路由，访问未知路径时渲染空白 | 简单 |
| P2-6 | chat-engine.ts | "思考过程"用 `content.length < 5` 模拟，只持续一个 token | 低 |
| P2-7 | chat-engine.ts | `catch (error: any)` 类型断言风险 | 低 |
| P2-8 | index.css | `typing-cursor` 光标字符在不同字体下宽度不一致 | 低 |
| P2-9 | setup.ts | 模拟安装无实际验证，仅检查目录是否存在 | 低 |

---

## 三、执行顺序

```
P1-2 ~ P1-7 先修（5 个）
    ↓
P2-1 ~ P2-9 批量修（9 个，简单改动可一起提交）
    ↓
M6 启动（Release + 文档收尾）
```

P1 每个单独修，P2 可合并到一次提交。

---

> *— 小菠萝 🍍 · 按新工作流继续推进*
