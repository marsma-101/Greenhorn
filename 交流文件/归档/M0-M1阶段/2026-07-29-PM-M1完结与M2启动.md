# PM 确认 · M1 完结 & M2 启动

> 发出人：小菠萝 🍍（PM）
> 接收人：技术主管（Trae）
> 日期：2026-07-29
> 状态：✅ M1 已关闭，M2 可启动

---

## 一、M1 验收结果

### ✅ 通过的项目

| 交付物 | 状态 |
|--------|------|
| 项目初始化（pnpm + React + Express） | ✅ 完成 |
| 前端 4 个页面骨架 | ✅ 完成 |
| 后端 API 路由框架 | ✅ 完成 |
| SSE 流式对话通道 | ✅ 完成 |
| 7 家供应商预设数据 | ✅ 完成 |
| 数据目录选择页面 | ✅ 完成 |
| Ollama 检测 API | ✅ 完成 |
| 沟通规范建立 | ✅ 完成 |

### 🟡 移入 M2 的已知问题

以下问题经 PM 确认，**不阻塞 M1 关闭**，作为 M2 首批任务处理：

| 优先级 | 问题 | 对应文件 | 说明 |
|--------|------|---------|------|
| **P0** | Ollama 对话被模拟数据拦截 | `chat.ts:26` | `!apiKey` 判断把 Ollama 也挡住了 |
| **P0** | API Key 未传到后端 | `ChatPage.tsx:47` | getChatConfig() 没带 apiKey |
| **P0** | 对话历史硬编码为空 | `chat.ts:35` | 每次传 `[]`，AI 无上下文 |
| **P0** | 保存设置是空操作 | `config.ts:24` | PUT 路由只有 TODO |
| P1 | 模型下拉框不随供应商切换 | `SettingsPage.tsx:68` | 选 Ollama 还在显示 DeepSeek 模型 |
| P1 | 思考内容被丢弃 | `ChatPage.tsx:90` | thinking 的 content 未渲染 |
| P1 | 保存设置无反馈 | `SettingsPage.tsx:29` | 点保存看不到成功/失败 |
| P2 | PI 检测返回模拟数据 | `pi-check.ts` | 永远返回 installed: true |
| P2 | 配置读取返回模拟数据 | `config.ts` | 永远返回默认值 |

---

## 二、M2 目标

**让用户在浏览器 Ollama 上完成第一次完整的对话。**

### M2 任务顺序

```
第一批（修 P0，让东西能动起来）
├── 🔧 #1 Ollama 对话修复（chat.ts 绕过 apiKey 检查）
├── 🔧 #2 API Key 传递修复（ChatPage 发请求带上 Key）
├── 🔧 #3 对话历史传递（ChatPage 传 messages）
├── 🔧 #4 配置保存做实（config.ts 写入真实文件）

第二批（完善体验）
├── 📄 模型 API 引导页（供应商卡片→选模型→填 Key→验证）
├── 🔧 模型下拉框同步供应商切换
├── 🔧 保存设置加反馈（loading/success/error）
├── 🔧 数据目录接入后端真实逻辑

第三批（补全功能）
├── 📥 PI 镜像拉取（clone + npm install）
├── 🔧 真实 PI 配置文件读写（auth.json / models.json）
├── 🔧 思考内容渲染（可展开区块）
├── 🔧 高级设置入口按钮（内容留空）
```

### M2 验收标准

| # | 条件 | 说明 |
|---|------|------|
| 1 | 选 Ollama → 发消息 → 看到本地模型流式回复 | 链路通了 |
| 2 | 连续对话有上下文（AI 记得上一句） | 历史传了 |
| 3 | 设置页 Key 能保存 + 对话时能读到 | 配置持久化了 |
| 4 | 模型下拉框随供应商正确切换 | UI 联动正常 |
| 5 | 保存设置有反馈 | 用户体验完整 |

---

> *— 小菠萝 🍍 · M1 已关，M2 启动*
