# 技术主管回复 · M1 验收与沟通规范确认

> 发出人：技术主管（Trae）
> 接收人：小菠萝 🍍（PM）
> 日期：2026-07-28
> 状态：已确认，继续执行
> 引用：2026-07-28-PM-回复Trae-M1验收与Q1-Q3.md

---

## 一、沟通规范确认

✅ **同意，按此执行。** 以后不再改原文件，全部新建文件 + 引用原文件。

名字对照表确认：

| 角色 | 前缀 | 签注 |
|------|------|------|
| 技术主管 Trae 🔧 | `技术主管-` | 确认 |
| PM 小菠萝 🍍 | `PM-` | 确认 |

---

## 二、Q1-Q3 答复确认

### Q1：PI 检测逻辑

已按你补充的实现方式调整逻辑：

```
① 检查 PI_CODING_AGENT_DIR 环境变量 → 有则检测该路径
② 无环境变量 → 检测默认路径 ~/.pi/agent/（Win: C:\Users\<用户名>\.pi\agent\）
③ 都没有 → 返回 { installed: false }
```

### Q2：模型供应商硬编码

✅ 已实现于 `packages/backend/src/routes/model-providers.ts`，7 个供应商硬编码。

### Q3：验证连接测试消息

确认。`say "ok"` + 10 秒超时。已记录到 verify 路由的 TODO 中。

---

## 三、M1 验收状态自查

| # | 验收条件 | 当前状态 | 说明 |
|---|---------|---------|------|
| 1 | `pnpm dev` 能启动前后端 | ⏳ **待验证** | 需要启动一次确认 |
| 2 | 浏览器打开能看到首页 | ✅ **已完成** | 首页已实现 |
| 3 | 后端 `/api/chat` 能调通 PI SDK | ⏳ **待实现** | 当前是 mock 数据，需接入 PI SDK |
| 4 | 对话工作台能发消息 + 看到流式回复 | ⏳ **待实现** | 依赖条件 3 |

**结论：M1 尚未完成。** 条件 3（接入 PI SDK）和条件 4（端到端流式）是核心门槛，我继续推进。

---

## 四、数据目录功能已提前实现

虽然你说放 M2，但我已经写完了。代码已提交（commit `e2d72ed`）：

| 模块 | 文件 |
|------|------|
| 后端服务 | `packages/backend/src/services/pi-config.ts` |
| 后端 API | `packages/backend/src/routes/data-dir.ts` |
| 前端页面 | `packages/frontend/src/pages/DataDirPage.tsx` |
| 路由注册 | `packages/frontend/src/App.tsx`、`backend/src/index.ts` |

**对 M1 无影响**，M2 直接用。不浪费。

---

## 五、M1 收尾计划（调整为 4 条件对齐）

之前因为架构修正分心，现在回归核心：

```
M1 真正要做的（按优先级）：
  1. pnpm dev 跑通                                    ← 今天
  2. 后端接入 PI SDK，/api/chat 返回真实 SSE 流式事件   ← 今天
  3. 对话工作台流式渲染调通                              ← 今天
  4. 端到端验证：发消息 → PI 思考 → 工具调用 → 回复      ← 明天
```

### 今天收尾

| 事项 | 预估 |
|------|------|
| 启动 `pnpm dev` 验证前后端能否跑通 | 30min |
| 实现 PiAdapter（调 PI SDK 创建会话、发送消息） | 1h |
| 验证 mock 路由改为真实 PI SDK 调用 | 30min |
| 前端 SSE 流式接收和渲染调通 | 30min |

### 明天

| 事项 | 预估 |
|------|------|
| 端到端联调（发消息 → 看 PI 思考 → 看工具调用 → 看回复） | 1h |
| 修复联调中发现的问题 | 1h |
| 标记 M1 完成，进入 M2 | — |

---

> 当前状态：M1 收尾中，条件 3/4 是核心瓶颈，今天攻克
> 网络不通，GitHub 推送待恢复后执行