# PM 决策交付 · 给技术主管（Trae）

> 发出人：小菠萝 🍍（PM）
> 日期：2026-07-28
> 状态：产品 Owner 已确认，可执行

---

## 一、已确认的产品决策清单

| # | 事项 | 决策 |
|---|------|------|
| 1 | 项目名 | **GreenHorn**，GitHub 同名仓库已就绪 |
| 2 | 技术路线 | **React 18 + Vite + Tailwind CSS**（前端） / **Node.js + Express + TypeScript**（后端） / **SSE**（通信） / **pnpm**（包管理） |
| 3 | PI 源码访问 | ✅ **授权**。初期开发需要读 PI 源码了解接口与数据流。**但** → 见下方"硬约束" |
| 4 | 模型引导顺序 | MVP = **DeepSeek**（集成为主提供商），备选 = **Ollama (qwen3.5:9b)** |
| 5 | 通信方式（Q3） | **SSE**（Server-Sent Events），已确认 |
| 6 | PI 驱动备选（Q7） | **方案B** → 自实现��量对话引擎，**不碰任何开源智能体源码** |
| 7 | 会话存储（Q11） | **SQLite** |
| 8 | GitHub 操作 | 产品 Owner **直接跟你对接**技术实现细节 |
| 9 | 项目启动 | PM 与你对齐完成后 → **立即启动 M0** |

---

## 二、硬约束（产品 Owner 明确，不可违反）

### 🔴 约束 1：架构不能绑定 PI

> 后期会通过同一个前端挂接**其他开源智能体项目**（不止 PI）。

**要求**：M0 阶段就必须设计好 **Adapter 抽象层**。换引擎只需写一个新的 Adapter 文件，**前端和后端核心代码不需改动**。

### 🔴 约束 2：不能碰开源智能体源码

> 我们不能修改任何开源智能体的原始程序。（Q7 选择方案B的原因）

**要求**：
- M0 如果验证"网页驱动 PI"机制走不通 → **不准改 PI 源码**，直接切换方案B（自实现轻量对话引擎）
- 方案B的对话引擎只跟 PI 的 **OpenAI 兼容 API** 交互，不侵入 PI 内部
- 同理，后续新引擎也是通过 API / SDK 对接，**不修改对方代码**

### 🔴 约束 3：不能定得太死

初期虽然以 PI 做 MVP，但 **代码结构、配置方式、文档描述** 都要为"换引擎"留活口。避免后期重构大动干戈。

---

## 三、技术主管待办

### 优先：M0 技术验证

**核心验证项**："网页驱动 PI 对话"是否可行
- 读 PI 源码 → 确认其 API / 流式接口 / 工具调用机制
- 搭建最小原型：前端发消息 → 后端调 PI → 流式回复回到对话界面
- **如果走不通** → 验证方案B 可行性并切换

### 待你决策（由你直接与技术主管沟通）

Owner 原话：*"技术方面的问题交给他来做，咱们就管项目方向和完善上的问题。"*

以下由你直接跟 Trae 讨论：
- GitHub 仓库配置细节（Actions / Pages / Discussions 等）
- Q1-Q17 中其余技术选型细节（Q2 Express vs Fastify、Q4 pnpm 等）
- 子智能体 7 人分工方案的执行细节
- CI/CD 流水线的具体配置

---

## 四、启动信号

产品 Owner 说：

> *"你跟trae达成一致之后就可以开始了。"*

所以等你看到这份文档，确认技术方案能满足上述约束 → **回复我一声** → 我出 M0 的启动指令，技术验证直接开跑。

---

*—— 小菠萝 🍍*
