# PM 指令 · S1 验收通过，继续 S2 + S3（完成后一次性交付）

> 发出人：小菠萝 🍍（PM）
> 接收人：技术主管（Trae）
> 日期：2026-08-01 13:00
> 引用：2026-08-01-1330-技术主管-交付S1-UI层改造.md
> 状态：待执行

---

## 一、S1 验收结论：✅ 通过

S1（UI 层改造）验收通过：
- 首页引擎选择页 + 6 张卡片 ✅
- 路由 `/` → HomePage、`/chat` 新增 ✅
- PI 动态检测正常（实测 `/api/pi` 返回 `installed: true`）✅
- 编译通过 ✅

**无需再等布局确认，直接继续 S2。**

---

## 二、下一步：S2 + S3 全部完成后再交付

**执行方式变更**：不再分阶段交付验收，**S2、S3 连续做完，最后一次性交付**。

```
S1 ✅（已完成）
  ↓
S2：ai-agent 基础设施
  ├── ai-agent 目录 + README
  ├── 程序自身位置检测（C 盘提醒）
  ├── 引擎检测 API + 迁移三件套
  ├── 数据迁移 + 备份/恢复
  ├── 设置页数据管理界面
  └── 会话存储迁移（方案 A）
  ↓
S3：模板管理 + 技能管理
  ├── prompts API + 前端
  ├── 技能扫描 + 安全检测 + 技能库界面
  ├── 自动/手动调用模式
  └── 对话页 🧰 技能按钮
  ↓
一次性完整交付 + PM 全面验收
```

---

## 三、S2 执行要点（方案 A 重点确认）

### 3.1 会话存储迁移（方案 A）

会话路径从 `~/.pi/agent/sessions/` 迁移到 `ai-agent/pi/data/sessions/`：

**必须验证**：
1. 现有会话完整迁移（2 个迁移会话 + 测试会话）
2. 历史会话可正常打开
3. jsonl 格式与已验收格式**完全一致**：
   - message 行嵌套 `message` 对象
   - `session_info` 事件存会话名
   - `version: 3`
   - **cwd 真实路径，不双重转义**（上次教训，务必检查）
4. PI 的 `/resume` 能看到迁移后的会话

### 3.2 环境变量注入（逐个引擎实测）

- PI 支持 `PI_CODING_AGENT_DIR` ✅（已验证）
- Hermes / Claude Code / Codex / OpenCode / Reasonix：**逐个实测**
- 不支持的引擎用配置文件方式指定数据目录
- 验收时每个引擎都要真实测过，不能只测 PI

### 3.3 迁移原则（复制不删除）

- 迁移 = 复制到 ai-agent，原位置保留
- 用户确认新位置正常后，才允许删旧位置
- 失败可回退

---

## 四、S3 执行要点

1. 数据目录用 S2 的最终路径（`ai-agent/<引擎>/data/prompts`、`ai-agent/<引擎>/data/skills`）
2. 模板编辑器 = 直接编辑 md + 新建时生成参照模板
3. 技能库 4 个扩充入口：AI 生成 / 线上安装 / 本地安装 / 外部文件夹扫描
4. 自动/手动调用模式（settings 存 `skillMode`）
5. 对话页「🧰 技能」按钮（不受调用模式影响）
6. 含脚本技能显示 ⚠️ 安全提示（只提示不强审）

---

## 五、最终交付要求

**一次性交付**包含：
1. S2 + S3 全部功能完成报告
2. 完整的验收对照表（按计划中的 S2/S3 验收标准逐条）
3. 每个引擎的环境变量兼容性实测记录
4. 会话迁移验证记录（格式一致性 + /resume 可读）

全部完成后通知 PM 全面验收，验收通过前不进入下一批。

---

> *— 小菠萝 🍍 · S1 通过，S2+S3 一次做完，交付时给我完整验收报告*
